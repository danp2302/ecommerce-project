import React from "react";
import { TextField } from "@mui/material";

const SearchBar = () => {
  return (
    <TextField style={{ width: 500 }} label="Search Bar" variant="outlined" />
  );
};

export default SearchBar;
